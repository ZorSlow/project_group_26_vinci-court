/**
 * Remarque :
 *
 * La logique de remplacement des URLs de développement (remplacer
 * "http://localhost:3000/" par "/") a été extraite dans un script dédié
 * et ne doit PAS s'exécuter automatiquement.
 *
 * Lancez manuellement le script depuis la racine du projet :
 *
 *  - Dry run (n'applique rien, affiche les fichiers concernés) :
 *      node scripts/replace-localhost.js --dry-run
 *
 *  - Appliquer les modifications :
 *      node scripts/replace-localhost.js
 *
 * Ce fichier conserve uniquement cette note pour éviter toute exécution
 * automatique lors du démarrage de l'application. Si vous cherchez le code
 * effectif de remplacement, il se trouve dans le script de remplacement réel.
 */